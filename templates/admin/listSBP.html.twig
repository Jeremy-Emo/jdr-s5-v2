{% block content %}
    <div class="row">
        <div class="col ma-25-top">
            <a href="{{ path('admin_createSBP') }}"><button type="button" class="btn btn-primary">Créer un bonus de stat</button></a>
        </div>
    </div>
    <div class="row">
        <div class="col ma-25-top">
            <div class="pa-20 bg-white">
                <h1 class="ma-20-bottom">Liste des bonus de stat :</h1>
                <p class="ma-10-bottom">
                    <span>Nombre de bonus de stat : {{ statBonusPercents|length }}</span>
                    <span class="f-right"><input type="text" id="searchBar"></span>
                </p>
                <table class="table" id="stupidTable">
                    <thead>
                    <tr>
                        <th scope="col" data-sort="string" class="cursor-click"><i class="fas fa-caret-down"></i> Nom</th>
                        <th scope="col">Actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for sbp in statBonusPercents %}
                        <tr class="searchedItem" data-search="{{ sbp.__toString()|lower }}">
                            <td>{{ sbp.__toString() }}</td>
                            <td>
                                <a href="#">Modifier</a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}